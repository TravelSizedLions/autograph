name: Build Dev Branch
run-name: 'Build Dev: ${{github.sha}}'
on:
  pull_request:
    branches: ["dev"]

jobs:
  # Run Tests:
  #   id: run-tests
  #   uses: TravelSizedLions/actions/.github/workflows/merge-request.yaml@dev
  
  bump-versions:
    name: Bump ${{ github.event.repository.name }} addons.json patch version
    uses: ./.github/workflows/bump-addons.yaml

  # bump-plugin-version:
  #   name: Bump plugin.cfg version
  #   uses: ./.github/workflows/bump-plugin.yaml
